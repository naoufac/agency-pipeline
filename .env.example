# Board (Postgres / Supabase). Local docker default:
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5439/agency

# Agents — MiniMax (OpenAI-compatible). Leave MINIMAX_API_KEY unset to use offline stubs.
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.io/v1
MINIMAX_MODEL=MiniMax-M2
